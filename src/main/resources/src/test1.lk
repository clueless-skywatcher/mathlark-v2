<Add2> := {
    (a) := {
        Return a + Var('x') * Var('y');
    }
}

PrintLn(Add2(Var("x")));
PrintLn(Add2(2));

x := Var('x')
y := Var('y')
p1 := x^3 * y^2 - x^2 * y^3 + x
p2 := 3*x^4 * y + y^2
SPolynomial(p1, p2)